# TODO - Contact Form Email Integration

## Steps
- [x] Confirm approach: FormSubmit.co
- [x] Change `<form>` tag to use FormSubmit.co action + method POST
- [x] Add hidden fields (_subject, _template, _captcha) for better emails
- [x] Remove `onsubmit` handler and `handleContactSubmit` JS function
- [x] Add JS to set email subject from dropdown
- [ ] Test the form submission to confirm email arrives (user action)
